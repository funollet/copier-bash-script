_subdirectory: project
_templates_suffix: ""

_tasks:
  - "chmod +x [[ script_name ]]"

script_name:
  type: str
  default: "script.sh"

creates_tmp_file:
  type: bool
  default: yes
  help: Add code to manage a temporary file?


